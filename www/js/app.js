/**
 * app.js - basic App code goes here.
 */
(function(global) {
    "use strict";
    // Export as App
    if (global.App === undefined) {
       global.App = {};
    }
    // Export functions e.g. globalApp.myfunc = myfunc;
}(this));
